# Dependencias del motor de análisis. Versiones fijadas (rangos compatibles)
# para evitar que una actualización mayor rompa el cálculo sin aviso.
yfinance>=0.2.40,<0.3
pandas>=2.1,<3.0
numpy>=1.26,<3.0
